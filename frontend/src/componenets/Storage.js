import Todo from './Todo'

export default function Storage(task) {

    // chrome.storage.sync.set({task }).then(() => {
    //     console.log("Value is set to " + task);
    //   });

      

}